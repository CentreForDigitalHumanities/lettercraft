query ViewContributor($id: ID!) {
    userDescription(id: $id) {
        id
        fullName
        publicRole
        description
        caseStudies {
            id
            name
        }
        contributedSources {
            id
            name
        }
    }
}
