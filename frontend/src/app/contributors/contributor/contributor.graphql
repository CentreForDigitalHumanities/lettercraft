query ViewContributor($id: ID!) {
    userDescription(id: $id) {
        id
        fullName
        publicRole
        description
        picture
        caseStudies {
            id
            name
        }
        contributedSources {
            id
            name
        }
    }
}
