import { UniqueIDDirective } from './unique-id.directive';

describe('UniqueIdDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueIDDirective();
    expect(directive).toBeTruthy();
  });
});
