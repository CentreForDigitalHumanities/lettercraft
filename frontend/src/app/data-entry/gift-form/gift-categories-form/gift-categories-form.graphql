query DataEntryGiftCategories($id: ID!) {
    giftDescription(id: $id, editable: true) {
        id
        categories {
            id
            label
            description
        }
    }
}

query DataEntryAllGiftCategories {
    giftCategories {
        id
        label
        description
    }
}
