query DataEntryGiftCategories($id: ID!) {
    giftDescription(id: $id) {
        id
        categories {
            id
            label
            description
        }
    }
}

query DataEntryAllGiftCategories {
    giftCategories {
        id
        label
        description
    }
}
