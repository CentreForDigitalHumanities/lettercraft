query DataEntryLetterCategories($id: ID!) {
    letterDescription(id: $id) {
        id
        categories {
            id
            label
            description
        }
    }
}

query DataEntryAllLetterCategories {
    letterCategories {
        id
        label
        description
    }
}
