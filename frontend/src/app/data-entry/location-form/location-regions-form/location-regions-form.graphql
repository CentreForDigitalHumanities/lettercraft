query DataEntryLocationRegions($id: ID!) {
    spaceDescription(id: $id, editable: true) {
        id
        regions {
            id
        }
    }
}

query DataEntryRegions {
    regions {
        id
        name
        type
    }
}
