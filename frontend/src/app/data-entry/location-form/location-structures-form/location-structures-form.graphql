query DataEntryLocationStructures($id: ID!) {
    spaceDescription(id: $id, editable: true) {
        id
        structures {
            id
        }
    }
}

query DataEntryStructures {
    structures {
        id
        name
        level
        settlement {
            id
            name
        }
    }
}
