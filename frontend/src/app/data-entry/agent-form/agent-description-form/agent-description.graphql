query DataEntryAgentDescription($id: ID!) {
  agentDescription(id: $id) {
    id
    designators
    gender {
      id
      gender
      sourceMention
      note
    }
    source {
        id
    }
  }
}
