query DataEntryAgentHistoricalPerson($id: ID!) {
    agentDescription(id: $id, editable: true) {
        id
        isGroup
        describes {
            id
        }
    }
}

query DataEntryHistoricalPersons {
    historicalPersons {
        id
        name
    }
}
