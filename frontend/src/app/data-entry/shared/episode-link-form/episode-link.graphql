query EpisodeAgentQuery($id: ID!) {
    episodeAgentLink(id: $id) {
        id
        note
        sourceMention
        episode {
            id
            name
        }
        agent {
            id
            name
        }
    }
}
