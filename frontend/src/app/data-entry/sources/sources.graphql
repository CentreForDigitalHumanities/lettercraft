query DataEntrySourceList {
    sources(editable: true) {
        id
        name
        editionAuthor
        editionTitle
        medievalAuthor
        medievalTitle
        numOfEpisodes
    }
}
