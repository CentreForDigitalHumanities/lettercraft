query ViewCaseStudy($id: ID!) {
    caseStudy(id: $id) {
        id
        name
        authors {
            id
            fullName
        }
        date
        content
        sources {
            id
            name
        }
    }
}
