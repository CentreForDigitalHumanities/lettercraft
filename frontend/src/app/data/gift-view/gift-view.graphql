query ViewGift($id: ID!) {
  giftDescription(id: $id) {
    id
    name
    description
    editable
    source {
        id
        name
        isPublic
    }
    episodes {
        id
        sourceMention
        designators
        note
        episode {
            id
            name
        }
    }
    contributors {
        id
        fullName
    }
  }
}
