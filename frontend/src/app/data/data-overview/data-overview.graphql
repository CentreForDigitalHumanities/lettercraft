query ViewSources($search: String) {
    sources(search: $search, publicOnly: true) {
        id
        name
        editionAuthor
        editionTitle
        medievalAuthor
        medievalTitle
        numOfEpisodes
    }
}
