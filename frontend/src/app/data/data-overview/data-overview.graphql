query ViewSources($search: String) {
    sources(isPublic: true, search: $search) {
        id
        name
        editionAuthor
        editionTitle
        medievalAuthor
        medievalTitle
        numOfEpisodes
    }
}
