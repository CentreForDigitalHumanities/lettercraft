query ViewEpisodes($search: String) {
  episodes(search: $search) {
    id
    name
    summary
    categories {
        id
        name
    }
    book
    chapter
    contributors {
      id
      fullName
    }
    page
    source {
      id
      name
    }
  }
}
